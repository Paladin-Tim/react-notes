specVersion: "v1beta"
serviceId: "react-todo"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "react-todo-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
